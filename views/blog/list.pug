extends ../layout

block content
  h1 Student Don Afable's Blog Site
  ul
    each post in posts
      li
        a(href=`/blog/${post._id}`) #{post.title}
        p #{post.author}
        p #{post.createdAt.toDateString()}
